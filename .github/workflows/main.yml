name: Upload
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Server 🚀
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@main
      - name: Upload to server
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: zedruc.net
          username: markus
          password: ${{ secrets.ftp_password }}
          protocol: ftps
          local-dir: ./public/
          server-dir: /home/markus/servers/zedruc_net/website/atc24-suite
